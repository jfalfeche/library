import { FC } from 'react'
import { SectionProps } from '@app/types'

import classNames from 'classnames'

const Contact: FC<SectionProps> = ({ styles }) => {
  return <div className={classNames(styles)}>contact</div>
}

export default Contact
